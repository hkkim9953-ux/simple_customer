rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 서버(Admin SDK)는 규칙 우회. 클라이언트 직접 접근은 기본적으로 차단.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
